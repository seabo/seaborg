[package]
name = "engine"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
core = { path = "../core" }
crossbeam-channel = "0.5.16"
separator = "0.4"
open = "5.4.0"

[dev-dependencies]
rand = "0.10.2"
