[package]
name = "engine"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
core = { path = "../core" }
crossbeam-channel = "0.5.15"
separator = "0.4"
log = "0.4"
open = "4.0.0"

[dev-dependencies]
rand = "0.8"
